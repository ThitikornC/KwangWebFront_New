[phases.setup]
nixPkgs = ['nodejs_18', 'git']

[phases.install]
cmds = ['npm ci', 'npm install -g @railway/cli']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node .output/server/index.mjs'
